# Yuhang Xie — Personal Academic Homepage

谢宇航的个人学术主页，部署于 GitHub Pages。

这是一个无构建依赖的静态单页站点：主页内容位于 `index.html`，样式位于 `assets/css/style.css`。推送到默认分支后即可由 GitHub Pages 发布。

## 本地预览

在仓库目录启动任意静态文件服务器，例如：

```bash
python -m http.server 4000
```

然后访问 `http://localhost:4000`。
